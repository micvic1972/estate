import {PrismaClient} from "@prisma/client"
//so this help talk with prisma to the auth controller and it will allow us to perform database operations such as creating, reading, updating, and deleting data in the database through the prisma client instance that we created and it will be used in the controller to interact with the database and perform CRUD operations on the user data and it will also allow us to handle any errors that may occur during the database operations and it will help us to manage the connection to the database and ensure that it is properly closed when we are done with it and it will also help us to manage the connection pool and ensure that we are not exceeding the maximum number of connections allowed by the database server and it will also help us to manage transactions and ensure that they are properly committed or rolled back in case of any errors that may occur during the transaction process and it will also help us to manage migrations and ensure that our database schema is up to date with our application code and it will also help us to manage seeding and ensure that our database is populated with initial data for testing or development purposes and it will also help us to manage logging and ensure that we are logging any errors or important events that may occur during our application runtime for debugging or monitoring purposes
const Prisma = new PrismaClient() //this is what connect the prisma client to the database and it allow us to perform database operations such as creating, reading, updating, and deleting data in the database through the prisma client instance that we created and it will be used in the controller to interact with the database and perform CRUD operations on the user data and it will also allow us to handle any errors that may occur during the database operations and it will help us to manage the connection to the database and ensure that it is properly closed when we are done with it and it will also help us to manage the connection pool and ensure that we are not exceeding the maximum number of connections allowed by the database server and it will also help us to manage transactions and ensure that they are properly committed or rolled back in case of any errors that may occur during the transaction process and it will also help us to manage migrations and ensure that our database schema is up to date with our application code and it will also help us to manage seeding and ensure that our database is populated with initial data for testing or development purposes and it will also help us to manage logging and ensure that we are logging any errors or important events that may occur during our application runtime for debugging or monitoring purposes

export default Prisma; //this is what export the prisma client instance so that it can be imported and used in other parts of the application such as the controller to perform database operations and it will allow us to keep our code organized and modular by separating the database connection logic from the business logic in the controller and it will also allow us to easily manage and maintain our database connection throughout the application lifecycle and it will also help us to ensure that we are using a single instance of the prisma client throughout the application to avoid any issues with multiple connections or inconsistent data access and it will also help us to ensure that we are properly handling any errors that may occur during database operations and it will also help us to ensure that we are properly closing the database connection when we are done with it to avoid any potential memory leaks or connection issues in the future and it will also help us to ensure that we are properly managing our database transactions and ensuring that they are properly committed or rolled back in case of any errors that may